#include <fstream>
#include <iostream>
#include <sstream>
#include <vector>
using namespace std;

void writeToFile(string file, vector<vector<string>> data);
vector<vector<string>> readFromFile(string file);